#!/usr/bin/env bash

# Stop VNC server
vncserver -kill :1 2>/dev/null

# Fix: use wildcard to remove all pid/log files under $HOME/.vnc
rm -f "$HOME/.vnc/"*.pid
rm -f "$HOME/.vnc/"*.log

# Fix: remove stale X lock files
rm -f /tmp/.X1-lock
rm -f /tmp/.X11-unix/X1

echo "VNC Server stopped."
